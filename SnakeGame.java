public class SnakeGame {

  public static void main(String[] args) {

    // GameFrame frame = new GameFrame();
    // Didn't name because we don't need to reference the name
    new GameFrame();

    // wrap around the wall mechanic?
    // bug fix: inputting directions too fast causes a suicide
    // change red circle to apple jpg?
    // obstacles that come down every once in a while that you have to avoid
    // differentpowerups clear screen, activate wall passthrough
    // split the snake into halves?
    // Color modes for snake?
    // startup menu? Allow click to play again
  }
}